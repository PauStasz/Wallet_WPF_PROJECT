<UserControl x:Class="Wallet.Themes.UserControls.SidebarMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wallet.Themes.UserControls" 
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:viewModels="clr-namespace:Wallet.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="170">

    <UserControl.DataContext>
        <viewModels:SidebarMenuViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#272829">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition MinHeight="30"/>
                    <RowDefinition MinHeight="30"/>
            </Grid.RowDefinitions>


                <Label  Grid.Row="0" Height="90" Margin="2 30 2 2" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <svgc:SvgViewbox Source="/Assets/Vectors/person-account.svg" />
                </Label>

                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
            </Grid>

            <StackPanel Grid.Row="1">
            <Button x:Name="HomeButton" Content="HOME" Style="{StaticResource lineBttn}" IsDefault="True" Click="GoHome"/>
            <Button x:Name="ExpensePlanningButton" Content="PLANOWANIE WYDATKÓW" Style="{StaticResource lineBttn}" Click="GoExpensePlanning" />
            <Button x:Name="RaportButton" Content="RAPORTY I STATYSTKI" Style="{StaticResource lineBttn}" Click="GoRaport" />
            <Button x:Name="ForecastButton" Content="PROGNOZA" Style="{StaticResource lineBttn}" Click="GoForecast" />
            <Button x:Name="AccountButton" Content="KONTA" Style="{StaticResource lineBttn}" Click="GoAccount" />
            <Button x:Name="CategoriesButton" Content="KATEGORIE" Style="{StaticResource lineBttn}" Click="GoCategories" />
            <Button x:Name="SettingsButton" Content="USTAWIENIA" Style="{StaticResource lineBttn}" IsDefault="False" Click="GoSettings" />
            <Button Content="WYLOGUJ SIĘ" Command="{Binding LogoutCommand}" Style="{StaticResource lineBttn}" />
        </StackPanel>

    </Grid>
</UserControl>
